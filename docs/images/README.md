# 界面截图

根 README 的「界面预览」章节引用本目录图片。所有截图**均已脱敏**（账号邮箱、私有域名、
Tunnel Token、局域网 IP、真实用户名、密钥值等已替换为示例值）：

| 文件 | 页面 |
| --- | --- |
| `api-keys.png`          | API 密钥：同一 Key 多方案切换 + 客户端配置 |
| `api-keys-fallback.png` | 备选 Provider 配置（优先级排序） |
| `overview.png`          | 接入地址（三协议端点） |
| `providers.png`         | Provider 列表 / 卡片 |
| `public-url.png`        | 公网访问（Cloudflare） |
| `usage.png`             | 用量统计 |
| `users.png`             | 用户管理 |
| `self-check.png`        | 自检 |

> 更新截图务必先脱敏（不含密钥 / Tunnel Token / 账号邮箱 / 私有域名 / 真实用户名），
> 再替换同名文件。
