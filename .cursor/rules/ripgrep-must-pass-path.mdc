---
description: 在 Shell 里用 rg/ripgrep 必须带搜索路径，否则会永久阻塞
alwaysApply: true
---

# ripgrep 在 Shell 中必须指定搜索路径

## 踩坑现象
在非交互 Shell 里运行**不带搜索路径**的 `rg`（如 `rg -n "xxx" --glob '*.go'`）会永久卡住：
CPU 0%、无输出、直到超时或被 kill。**不是** `.cache`/`node_modules` 太大，也不是特殊文件导致。

## 根因
rg 未指定搜索路径时，会判断 stdin 是否为终端。非交互 Shell 里 rg 的 stdin 是**管道**（非 TTY），
于是 rg 认为要搜 stdin 内容，阻塞在 `read(stdin)`。该管道永不来数据也不关闭 → 永久挂起。

对照：带路径的 `rg -n "xxx" internal` 秒回；不带路径的 `rg -n "xxx"` 必卡。

## 正确做法
1. 优先使用 Grep 工具（内部正确处理路径，不会触发此问题）。
2. 确需在 Shell 用 rg 时，**一律带上搜索路径**（末尾 `.` 或具体目录）：

```bash
# ❌ BAD：无路径，会永久阻塞
rg -n --glob '*.go' "AppendRequestLog"

# ✅ GOOD：显式给路径
rg -n --glob '*.go' "AppendRequestLog" .
rg -n "AppendRequestLog" internal

# ✅ 兜底：重定向 stdin
rg -n "AppendRequestLog" < /dev/null
```
